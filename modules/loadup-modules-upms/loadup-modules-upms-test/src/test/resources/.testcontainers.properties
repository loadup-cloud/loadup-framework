# Testcontainers Configuration
# Docker Host configuration for OrbStack
docker.host=unix:///Users/lise/.orbstack/run/docker.sock
# Enable container reuse to speed up tests
testcontainers.reuse.enable=true
# Disable Ryuk container (resource reaper) if needed
# testcontainers.ryuk.disabled=false

