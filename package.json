{
  "name": "loadup-docs",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "mkdocs-serve": "mkdocs serve -a 0.0.0.0:8000",
    "mkdocs-build": "mkdocs build",
    "clean-docs": "rm -rf site/"
  },
  "devDependencies": {},
  "dependencies": {}
}
