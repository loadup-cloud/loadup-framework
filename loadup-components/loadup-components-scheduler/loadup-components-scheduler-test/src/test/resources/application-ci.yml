spring:
  application:
    name: scheduler-test
  quartz:
    job-store-type: memory
    properties:
      org.quartz.threadPool.threadCount: 2

jobrunr:
  background-job-server:
    enabled: true
    poll-interval-in-seconds: 5
    worker-count: 2
  dashboard:
    enabled: false

testcontainers:
  reuse:
    enable: false

logging:
  level:
    root: WARN
    io.github.loadup.components.scheduler: INFO
