spring:
  application:
    name: gotone-test

loadup:
  database:
    logical-delete:
      enabled: true
  flyway:
    enabled: true
    table: flyway_schema_history_gotone

logging:
  level:
    io.github.loadup.components.gotone: DEBUG
