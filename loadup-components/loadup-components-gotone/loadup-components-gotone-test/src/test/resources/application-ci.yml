spring:
  application:
    name: gotone-test

loadup:
  database:
    logical-delete:
      enabled: true
  flyway:
    enabled: true
    table: flyway_schema_history_gotone

testcontainers:
  reuse:
    enable: false

logging:
  level:
    root: WARN
    io.github.loadup.components.gotone: INFO
